diff --git a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
index d77ca60..a4e56d5 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
@@ -40,6 +40,13 @@ import { useScheduleUpdate } from "./useScheduleUpdate.js";
 // This is to make TypeScript compiler happy
 // eslint-disable-next-line @typescript-eslint/no-unused-expressions
 import { jsx as _jsx } from "react/jsx-runtime";
+
+// Hermes (Android, legacy bridge): `WeakRef` global is missing. Navigation only needs `.deref()` for `===` checks.
+function createNestedParamsConsumableRef(params) {
+  return {
+    deref: () => params
+  };
+}
 PrivateValueStore;
 const isScreen = child => {
   return child.type === Screen;
@@ -388,7 +395,7 @@ export function useNavigationBuilder(createRouter, options) {
     if (setConsumedParamsRef && didConsumeNestedParams && typeof route?.params === 'object' && route.params != null) {
       // Track whether the params have been already consumed
       // Set it to the same object, so merged params can be handled again
-      setConsumedParamsRef(new WeakRef(route.params));
+      setConsumedParamsRef(createNestedParamsConsumableRef(route.params));
     }
   }, [didConsumeNestedParams, route?.params, setConsumedParamsRef]);
   const shouldUpdate = state !== nextState;
